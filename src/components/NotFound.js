import React from 'react';

const NotFound = () => (
  <div id="not-found">
    <div className="container">
      <p>
        Pague not found. Click <a href="/">here</a> to get back.
      </p>
    </div>
  </div>
);

export default NotFound;
